plugins {
    id "com.android.application"
}

android {
    namespace "app.socialfuse.twa"
    compileSdk 35

    defaultConfig {
        applicationId "app.socialfuse.twa"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }
}

dependencies {
    implementation "androidx.browser:browser:1.8.0"
}
